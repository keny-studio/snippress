// Add scripts files

add_action( 'wp_enqueue_scripts', 'keny_enqueue_scripts' );

function keny_enqueue_scripts() {
       wp_enqueue_script(
        'keny',
        get_stylesheet_directory_uri() . '/js/keny.js');}
